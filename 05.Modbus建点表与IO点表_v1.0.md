# 小型二供泵房数字孪生 · Modbus 建点表与 I/O 点表

**项目：小型二供泵房数字孪生监控系统　｜　版本：v1.0　｜　日期：2026-09-24　｜　编制：组长（兼 PM）**

---

## 一、这份文档给谁用

| 谁 | 拿去干什么 |
|---|---|
| ① 传感器与设备模拟工程师 | 照第二节这张表，在 Modbus 模拟器里**逐条建点** |
| ② 协议解析工程师 | 照这张表写"地址 → 标签名 + 缩放"的映射代码 |
| ③ 后端 / ⑤ 前端 | 不用直接看地址，但要知道每个测点**正常范围**是多少 |

**本表与《接口规范与数据字典 v1.1》第四节的关系**：那份表只给了「地址 + 标签名 + 缩放系数」，**没给寄存器存值的上下限**——建点的人不知道该填多少才算合法。本表把它补齐了，并加了建点注意事项。所以：

> **地址、标签名、缩放系数以《接口规范 v1.1》为准；存值范围与建点方法以本表为准。** 两者不冲突，本表是前者的展开。

---

## 二、保持寄存器（数值区，只读用：模拟器 → 协议解析）

| 地址 | 标签名 | 测点说明 | 单位 | 工程量程 | **寄存器存值范围** | 缩放 | 备注 |
|---|---|---|---|---|---|---|---|
| 40001 | tank_level | 水箱当前液位 | cm | 0 ~ 150.0 | **0 ~ 1500** | ÷10 | 存 855 = 实际 85.5 cm |
| 40002 | pipe_pressure | 出水总管压力 | MPa | 0 ~ 1.0 | **0 ~ 1000** | ÷1000 | 存 420 = 实际 0.420 MPa |
| 40003 | pump_01_status | 1#泵状态 | — | 0/1/2 | **0 ~ 2** | ×1 | 0=停止, 1=运行, 2=故障 |
| 40004 | pump_01_freq | 1#泵运行频率 | Hz | 0 ~ 50.0 | **0 ~ 500** | ÷10 | |
| 40005 | pump_01_current | 1#泵运行电流 | A | 0 ~ 20.0 | **0 ~ 200** | ÷10 | |
| 40006 | pump_01_temp | 1#泵电机温度 | ℃ | 0 ~ 120.0 | **0 ~ 1200** | ÷10 | |
| 40007 | pump_02_status | 2#泵状态 | — | 0/1/2 | **0 ~ 2** | ×1 | 0=停止, 1=运行, 2=故障 |
| 40008 | pump_02_freq | 2#泵运行频率 | Hz | 0 ~ 50.0 | **0 ~ 500** | ÷10 | |
| 40009 | pump_02_current | 2#泵运行电流 | A | 0 ~ 20.0 | **0 ~ 200** | ÷10 | |
| 40010 | pump_02_temp | 2#泵电机温度 | ℃ | 0 ~ 120.0 | **0 ~ 1200** | ÷10 | |
| 40011 | sys_mode | 系统模式 | — | 0/1 | **0 ~ 1** | ×1 | 0=手动, 1=自动 |
| 40012~40013 | today_kwh | 今日累计耗电量 | kWh | 0 ~ 500.0 | **Float32 双字** | **不缩放** | ⚠️ 全表唯一特例，见第五节 |
| 40014 | valve_inlet_opening | 进水总阀开度 | % | 0 ~ 100.0 | **0 ~ 1000** | ÷10 | 可选测点，待 PM 决策 |
| 40015 | pipe_flow_speed | 出水主管流速 | m/s | 0 ~ 3.0 | **0 ~ 300** | ÷100 | 可选测点，待 PM 决策 |
| 40016~40019 | — | **预留空号** | — | — | — | — | 故意留空，别占用 |
| 40020 | pump_01_freq_setpoint | 1#泵频率给定值 | Hz | 0 ~ 50.0 | **0 ~ 500** | ÷10 | **后端写入**，不是模拟器产生的 |
| 40021 | pump_02_freq_setpoint | 2#泵频率给定值 | Hz | 0 ~ 50.0 | **0 ~ 500** | ÷10 | 同上 |

---

## 三、线圈（开关量区，写入用：后端 → 模拟器）

| 地址 | 标签名 | 说明 |
|---|---|---|
| 00001 | pump_01_cmd_start | 写 1 启动 1# 泵 |
| 00002 | pump_01_cmd_stop | 写 1 停止 1# 泵 |
| 00003 | pump_02_cmd_start | 写 1 启动 2# 泵 |
| 00004 | pump_02_cmd_stop | 写 1 停止 2# 泵 |
| 00005 | sys_cmd_set_mode | 配合 40011 切换手/自动模式 |

**线圈的正确行为**：模拟器收到 `00001 = 1` 后，应当**执行动作**（把 40003 改成 1），然后**把线圈自己复位成 0**。否则后端下次再写 1 时，模拟器分不清是"新的一次启动"还是"上次的残留"。

---

## 四、报警阈值（后端判定，不属于建点内容）

这三个阈值由**后端**判定后推送标志位，模拟器和前端都不判断。列在这里只是为了让①号角色知道**造数据时应该造出能触发报警的数值**，方便联调。

| 报警 | 触发条件 | 对应标签 |
|---|---|---|
| 液位过低 | `tank_level < 25` cm | alarm_level_low |
| 液位过高 | `tank_level > 140` cm | alarm_level_high |
| 压力过高 | `pipe_pressure > 0.8` MPa | alarm_pressure_high |

> 液位过高阈值 140cm 是 PRD v1.1 的**暂定值**，待 PM 最终确认（水箱总高 150cm，留 10cm 余量）。

---

## 五、建点的 4 个坑（新手必踩，逐条对照）

### 坑 1：地址计数从 0 开始

Modbus 文档里写 `40001`，但**代码里的地址是 0**。这不是笔误，是 Modbus 的两套叫法：

| 文档写法（人看的） | 代码里的地址（程序用的） | 寄存器类型 |
|---|---|---|
| 40001 | **0** | 保持寄存器 |
| 40002 | **1** | 保持寄存器 |
| 00001 | **0** | 线圈 |
| 00002 | **1** | 线圈 |

**记住一句话：文档地址减 1，就是代码地址。** pymodbus 用的是 0-based 这套。如果你按 40001 去读，会读到 40002 的值，而且不会报错——这是最阴的一个坑。

### 坑 2：today_kwh 是全表唯一的双字（Float32）

其他 15 个测点都是"存整数 × 缩放系数"，只有 `today_kwh` 走 IEEE754 32 位浮点，**占两个寄存器**（40012 和 40013），**高字在前**。

- 建点时要占两个寄存器，不能只占 40012
- 协议解析要单独写一段解析，**不能套用 ÷10 那套逻辑**
- 后端读出来直接就是浮点数，不用再除

### 坑 3：保持寄存器存不了小数

Modbus 寄存器是 16 位整数，存不了 85.5 这种小数。所以要**先乘 10 再存**（存 855），读出来再**除以 10 还原**。

**这个换算由②协议解析负责**，模拟器只管存整数，后端和前端拿到的永远是还原后的真实值。

### 坑 4：40016~40019 是故意留空的

从 40015 直接跳到 40020，中间空 4 个。**不要为了"看起来连续"把它们用掉**——将来要加测点（比如补上水质），就从这里开始加，老地址一个都不用动。

---

## 六、建完怎么验证（用 QModMaster）

建完点不算完，要**读出来看一眼**才算数。

1. 打开 `D:\tools\QModMaster\qModMaster\qModMaster.exe`
2. 连接设置：Mode 选 **TCP**，IP 填 `127.0.0.1`，Port 填 `502`
3. 点 **Connect**
4. 在下面的 Slave ID 填 `1`，Function 选 `Read Holding Registers (0x03)`，Address 填 `0`，Quantity 填 `16`，点 **Send**
5. 看返回的数据区：第 1 行第 1 个应该是 **855**（因为 40001 存的是 85.5cm × 10）

**看到 855 就说明建对了。** 如果读出来是 0 或者报错，回头查坑 1（地址是不是写成了 1）。

---

## 七、建完要不要回填《接口规范》

要，但**本表已经替你对齐了**。

《接口规范 v1.1》第四节写着"本表地址为建议值，需传感器工程师在模拟器中实际建点后回填确认"。因为**本表的地址与接口规范完全一致**（只是补了存值范围），所以①号角色建点时**照本表建即可，不需要改地址**，建完在第 2 周对齐会上确认一句就行。

如果将来确实改了地址（比如模拟器有地址限制），**必须同时改《接口规范》并通知②③号角色**——地址是双方契约，不许单方面改。

---

## 八、版本记录

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| v1.0 | 2026-09-24 | 组长（兼 PM） | 初始版本。以《接口规范与数据字典 v1.1》第四节为基础，补齐寄存器存值范围；新增建点注意事项、验证方法、报警阈值汇总 |
