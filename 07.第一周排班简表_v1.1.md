# 泵房数字孪生 · 第一周排班（简版）

> 详细版见《泵房数字孪生_第一周工作排班表 v1.3.docx》。这份是**发群里用的简版**，只留每个人"今天该干嘛"。

---

## 本周只干一件事

**每人把工具装好，跑通一个最小例子，周日把它传进仓库。**

本周**不出功能、不做演示系统**——这是正常的，不用慌。三个月 12 周，第一周就是打地基。

---

## 逐日安排

### 周四 · 装环境

| 角色 | 今天做什么 |
|---|---|
| 产品经理 | 开启动会（讲清要做什么 + 这周只求入门）；发《统一字段命名表》到群里 |
| 传感器 | 装 ModRSsim2，能手动改数值就算成功 |
| 协议解析 | 装 Python；`pip install pymodbus`，跑通"读取寄存器"示例 |
| 后端 | 装 Python + FastAPI，浏览器能打开一个显示 hello 的页面 |
| 三维 | 装 Blender，跟着教程建出一个立方体 |
| 前端 | 装 Node（编辑器自选），让 Three.js 官网第一个示例在屏幕中央转起一个方块 |

### 周五 · 继续入门 + 集中解决装不上的

- 产品经理：**收集"谁卡在哪"**，组织一次集中答疑
- 各角色：把昨天没装好的补上，继续跟教程练手

### 周六 · 互相讲一遍 ← **本周最重要的一天**

每人 5 分钟，口头回答三个问题：

1. **我这一环负责什么？**（一句话）
2. **我的"输入"是谁给的？长什么样？**
3. **我的"输出"要给谁？长什么样？**

产品经理主持，会后整理一份《链路接口草表》：谁给谁、给什么格式。

> 这一天的产出**不是代码，是共识**。零基础团队最容易出的问题就是各干各的、最后拼不起来。周六做扎实了，第 3 周串线能少踩一半的坑。

### 周日 · 补进度 + **上传小 demo** ← 本周唯一的必达项

| 事项 | 内容 |
|---|---|
| **全员【必达】** | 把本周跑通的 demo 传到项目仓库 `demo/第1周/<你的角色名>/` |
| 传什么 | **截图或录屏**（证明真的跑通了）+ 代码或文件 |
| 怎么传 | 见下方「怎么把 demo 传上去」——**不会 git 也完全没问题** |
| **产品经理【必达】** | 当天检查仓库，确认六个人都传了；没收齐的在群里催一次 |

其余时间弹性，谁慢谁补。

### 周一 · 内部对齐会

逐个过：**工具装好了吗？最小例子跑通了吗？demo 传了吗？还卡在哪？**

产品经理会后发《第 2 周任务清单》到群里，白纸黑字，避免口头说完就忘。

### 周二 · 彩排（全员到场）

产品经理写讲稿，下午带全员走一遍，确保不冷场、不超时。

### 周三上午 · 导师演示（全员到场）

演示内容是**环境就绪情况 + 12 周计划 + 各自跑通了什么**，不是演示一个能动的系统。

每人准备一句话，说清"我这一环是什么"。导师问到技术细节，由对应角色回答（比如问用什么协议，由协议解析的人答 `Modbus TCP`）。

---

## 怎么把 demo 传上去（零基础版）

**推荐用网页拖拽，不用装任何东西，也不用敲命令。**

1. 在浏览器打开仓库地址：`https://github.com/Belem357/-`
   （⚠️ 组长第一次建库时仓库名起成了一个横杠 `-`，地址看着怪，但是对的。**如果你想改名**，告诉组长去 GitHub 仓库的 Settings 里改，改完地址会变。）
2. 点进 `demo` 文件夹 → 点进 `第1周` → 点进以**你角色名**命名的文件夹（没有就点 `Add file` → `Create new file`，文件名里带上 `/` 就能顺便建文件夹）
3. 点 **Add file** → **Upload files** → 把你的截图/录屏/代码文件**直接拖进去**
4. 下方点绿色的 **Commit changes** —— 传完了

> 如果提示没有权限：说明组长还没把你加成仓库协作者。把 GitHub 账号名发群里，让组长加一下 **Collaborators** 就能传了。

---

## 三条铁律

1. **卡住超 1 小时就发群里问**，别一个人闷三天。
2. **每天 1~3 小时，别熬夜。** 这项目有 12 周，可持续比冲刺重要得多。
3. **记住自己在链路哪一环**：传感器 → 协议解析 → 后端 → 前端。

---

## 附：数据链路图（每人对号入座）

```
[传感器: 造数据] → [协议解析: 读出来+翻译成统一名字] → [后端: 存起来+推给网页] → [前端: 收到数据+让3D动起来]
                                                            ↑                        ↓
                                              [写 Modbus 线圈] ← ← ← ← ← [控制指令下发]

[三维: 提供 3D 模型文件] →
[产品经理: 定规矩(字段名)、盯进度、对接口、备演示 —— 贯穿全程]
```

---

## 第一周交的不是"功能"，是"六个人都跑起来了"

| 交付物 | 谁负责 |
|---|---|
| 六人环境全部可用（截图为证） | 全员 |
| **各自跑通的小 demo，已上传到项目仓库** | **全员** |
| 《统一字段命名表》发布 | 产品经理 |
| 《链路接口草表》（谁给谁什么格式） | 产品经理 |
| 12 周演示讲稿 | 产品经理 |

---

## 版本记录

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| v1.0 | 2026-09-24 | 组长（兼 PM） | 初始版本，依据排班表 v1.2 精简 |
| v1.1 | 2026-09-24 | 组长（兼 PM） | 周日改为「补进度 + 上传小 demo 到 GitHub 仓库」并列为全员必达；新增「怎么把 demo 传上去」零基础指引；前端一栏去掉对 VS Code 的限定 |
