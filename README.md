# 小型二供泵房数字孪生监控系统

**课程项目 · 6 人团队 · 工期 12 周 · 每周三上午向导师演示**

> 新人第一次打开这个仓库，**从这份 README 看起**。它会告诉你：你在链路哪一环、要看哪几份文件、怎么把代码跑起来。

---

## 一、这个项目在做什么

**一句话**：小区自来水管网压力不够，要在中途设个泵房二次加压。这个项目就是给这样一个泵房做一套**网页版实时监控系统**——设备状态在 3D 模型上跟着真实数据动，能远程启停水泵，越限会报警。

**数据链路**（对号入座，就知道自己在哪一环）：

```
[传感器: 造数据] → [协议解析: 读出来+翻译] → [后端: 存起来+推给网页] → [前端: 收到数据+让3D动起来]
                                                      ↑                        ↓
                                        [写 Modbus 线圈] ← ← ← [控制指令下发]

[三维: 提供 3D 模型文件] →
[产品经理: 定规矩(字段名)、盯进度、对接口、备演示 —— 贯穿全程]
```

> ⚠️ **本项目没有真实泵房**，所有设备由软件模拟器顶替。但 **Modbus 通信协议是真的保留**——将来接实体泵房，只需把连接地址从模拟器换成真 PLC 的 IP，代码不用改。

---

## 二、按你的角色，看哪几份

| 角色 | 必看 | 选看 |
|---|---|---|
| **① 传感器与设备模拟** | `快捷上手路径`（下面第四节）<br>`角色材料/传感器与协议解析/05.Modbus建点表` | `代码/modbus/README.md`<br>`共享文档/06.开发环境安装手册` 第 2、3 节 |
| **② 协议解析** | 同上 | `共享文档/03.接口规范与数据字典` 第三节（标签字典）、第四节（地址表） |
| **③ 后端** | `代码/modbus/backendGuide.md` | `共享文档/03.接口规范` 第五、六节（WebSocket / REST） |
| **④ 三维模型** | `共享文档/03.接口规范` 第二节（**节点命名规范**，命名对不上前端就加载不出来） | `共享文档/01.PRD` 第 4 周里程碑 |
| **⑤ 前端** | `代码/frontend/scene.html` | `共享文档/03.接口规范` 第三节（标签）、第五节（WebSocket）、第七节（错误码） |
| **⑥ 产品经理** | `共享文档/08.第一周工作排班表` | 全部文档 |

**所有人都要看**：`共享文档/01.PRD`（做什么、不做什么）、`共享文档/03.接口规范`（全组契约）、`共享文档/09.示意图`（一张图看懂全貌）。

---

## 三、文件怎么放的（为什么这么分）

```
泵房/
├── 共享文档/          ← 人人要看的，只存一份，谁都不许私自改副本
├── 角色材料/          ← 只有某个角色才用的
│   └── 传感器与协议解析/
├── 代码/              ← 能跑的东西
│   ├── modbus/        ← ① ② ③ 三个角色共用（分不开，故意放一起）
│   └── frontend/      ← ⑤ 前端
├── demo/第1周/        ← 每人第 1 周的小 demo 传这里
├── 资料/              ← 课程/导师给的参考材料，不是我们的产出
└── _工具/             ← 内部小工具
```

**为什么共享文档不按角色拆开？** 因为 `03.接口规范` 被 ① 看第四节、② 看第三节、③⑤ 看第五六节——它是全组契约，不是谁的私产。如果按角色拆成六份副本，改一处忘同步就变成六个版本。**你们文档里已经出过一次同类问题**（`device` vs `device_id`、`action` vs `command` 两处字段名冲突，本质就是同一件事有两个版本）。所以共享的集中放，角色独占的才分开。

---

### 共享文档一览（编号即阅读顺序）

| 文件 | 是什么 | 谁维护 |
|---|---|---|
| `01.需求规格说明书PRD_v1.2.docx` | **最上位文档**。做什么、不做什么、验收标准、12 周里程碑、风险 | PM |
| `02.核心交互流程图_v1.1.docx` | 用户操作和系统内部怎么一步步流转 | PM |
| `03.接口规范与数据字典_v1.1.1.docx` | **全组契约**。字段名、Modbus 地址、WebSocket 格式、错误码 | PM |
| `04.项目验收清单Checklist_v1.1.docx` | 83 项验收标准。**判定做没做成的唯一依据** | PM |
| `06.开发环境安装手册_v1.1.docx` | 装什么软件、怎么验证、卡住了怎么办 | 组长 |
| `07.第一周排班简表_v1.1.docx` | 发微信群用的简版排班 | PM |
| `08.第一周工作排班表_v1.3.docx` | **详细版排班**，逐日到每个角色的每件事 | PM |
| `09.泵房设备与数据链路示意图.html` | 一张图看全设备与数据流。浏览器打开即可，可 Ctrl+P 存 PDF | 组长 |

> `05.Modbus建点表` 在 `角色材料/传感器与协议解析/` 下——它主要给 ① ② 用。
>
> `.md` 与 `.docx` 同名成对出现的，`.md` 是源稿、`.docx` 是成品。改内容改 `.md`，再重新生成 Word：
> ```bash
> python _工具/md2docx.py "共享文档/06.开发环境安装手册_v1.1.md" "共享文档/06.开发环境安装手册_v1.1.docx"
> ```

---

## 四、最短上手路径（① ② 号角色）

```bash
cd 代码/modbus
pip install -r requirements.txt      # 只装一个包，版本已锁死

# 窗口 1：启动模拟器（别关）
python sensorSim.py

# 窗口 2：验证链路
python linkCheck.py                  # 看到 13/13 通过就算通了
```

> ⚠️ **别直接 `pip install pymodbus`**。最新版把经典接口移除了，装上会报 `ImportError`。必须按 `requirements.txt` 锁 `3.6.9`。

**⑤ 前端**：浏览器直接打开 `代码/frontend/scene.html` 就行。

> ⚠️ 这个页面**必须联网**——它从 CDN 加载 Three.js 和 Vue。实测下载 `three.module.js` 要 9 秒，评审现场网络差或断网会**直接白屏**。**答辩前一定要把两个库下载到本地、改成相对路径引用。**

---

## 五、几条全组约定

1. **字段命名以 `03.接口规范与数据字典` 为唯一标准**。同一样东西不许有两个名字。
2. **Modbus 地址是双方契约**。① 号角色建点时照 `05.建点表` 建；确实要改地址，必须同时改 `03.接口规范` 并通知全组。
3. **报警由后端判定**，前端只负责显示。前后端各判一套会导致状态不一致（`03` 第 3.3 节）。
4. **卡住超过 1 小时就发群里问**，别一个人闷三天。
5. **每周三上午演示**，所以一周的节奏是「周四开工 → 下周三演示」。

---

## 六、代码推送到哪（两个仓库）

这个项目**同时推到两个地方**，内容完全一样：

| 远程名 | 地址 | 分支 | 谁要求 |
|---|---|---|---|
| `origin` | GitHub · `Belem357/-pump-station-digital-twin` | `main` | 我们自己用 |
| `gitee` | Gitee · `lichu-like/chizhouxueyuan25jike1ban8` | `master` | **老师要求** |

**一键推两个**：双击 `_工具/push-all.bat`（或在命令行跑它）。

> **为什么 Gitee 那边是 `master` 不是 `main`**：那个仓库是老师建的，默认分支就叫 `master`。我们本地的分支叫 `main`，所以推送命令写成 `git push gitee main:master`（把本地的 main 推成远程的 master）。
>
> **为什么第一次要 `--force`**：老师那个仓库建的时候生成了一个空白初始提交，和我们的历史没有共同祖先，不强推会被拒。**那次已经做完了**，之后正常推就行——`push-all.bat` 里用的是普通推送，没有 `--force`。

---

## 七、当前进度

| 阶段 | 周次 | 状态 |
|---|---|---|
| 一、打地基 | 第 1～2 周 | 🔵 **进行中**（第 1 周：装环境、跑通最小例子） |
| 二、通数据 | 第 3～4 周 | ⚪ 未开始 |
| 三、加功能 | 第 5～8 周 | ⚪ 未开始 |
| 四、补完善 | 第 9～12 周 | ⚪ 未开始 |

**第 1 周的目标不是做出功能**，是六个人都能说清"我在链路哪一环、上游给我什么、我要给下游什么"。做不出能演示的系统是完全正常的。
