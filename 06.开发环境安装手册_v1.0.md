# 泵房数字孪生 · 开发环境安装手册

**项目：小型二供泵房数字孪生监控系统　｜　版本：v1.0　｜　日期：2026-09-24　｜　发放：全组 6 人**

---

## 一句话说明

**每人装 2~3 样东西，装完能跑通一个最小例子，本关就过了。** 第一周不要求做出任何功能，装不上是正常的，卡住就发群里问。

---

## 第 0 步：所有人先做这个（3 分钟）

不配这个，后面下 Python 包会慢到你怀疑人生。

打开命令行（按 `Win + R`，输入 `cmd`，回车），粘贴这行，回车：

```
pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
```

> 如果提示 `pip 不是内部或外部命令`，说明 Python 还没装——先做下面第 1 步，装完 Python 再回来跑这行。

---

## 第 1 步：先装两样通用的

| 装什么 | 去哪装 | 装好怎么验证 |
|---|---|---|
| **Python** | https://www.python.org/downloads/ 下载 **3.14** 版 | 命令行输入 `python --version`，出现 `Python 3.14.x` |
| **编辑器**（自选一样即可） | VS Code：https://code.visualstudio.com/ ／ Cursor：https://cursor.com/ | 能打开就行 |

> ⚠️ 装 Python 时，安装界面**最底下有个勾选框 `Add Python to PATH`，一定要勾上**。不勾的话命令行找不到 python，后面全崩。这是新手第一号翻车点。

> ⚠️ **全组统一用 Python 3.14**。不要有人装 3.12、有人装 3.11——版本不一致会导致"我这儿能跑，你那儿报错"，排查起来非常痛苦。组长机器上就是这个版本，实测可用。

---

## 第 2 步：按自己的角色装

### ① 传感器与设备模拟

| | |
|---|---|
| 装什么 | **ModRSsim2**（图形化 Modbus 模拟器，1.5MB，免安装） |
| 去哪装 | https://sourceforge.net/projects/modrssim2/files/latest/download |
| 装好验证 | 打开它，界面里能看到一张寄存器表，手动改一个数值、再读到变化，就算过 |

> 为什么用 ModRSsim2 而不是写代码：它是**图形界面**，点点鼠标就能建点和改数值，零基础最友好。等以后熟练了再考虑用 pymodbus 写代码版模拟器。

### ② 协议解析

| | |
|---|---|
| 装什么 | Python 库 `pymodbus` + 调试工具 QModMaster |
| 怎么装 | 命令行输入：`pip install pymodbus` |
| 装好验证 | `python -c "import pymodbus; print(pymodbus.__version__)"`，出现版本号即可 |
| QModMaster | https://sourceforge.net/projects/qmodmaster/files/latest/download |

> QModMaster 是"上门查账的"——用来读寄存器、确认数据到底写进去没有。**模拟器（ModRSsim2）是开店铺的，QModMaster 是查账的**，两个都要有。

### ③ 后端

| | |
|---|---|
| 装什么 | FastAPI + uvicorn + websockets + SQLAlchemy |
| 怎么装 | `pip install fastapi uvicorn websockets sqlalchemy` |
| 装好验证 | 跑通 FastAPI 官网首页那个 5 行示例，浏览器打开 `http://127.0.0.1:8000/docs` 能看到接口页面 |
| 数据库 | **不用装**。用 SQLite，Python 自带 |

### ④ 三维模型

| | |
|---|---|
| 装什么 | **Blender**（建议 4.2 LTS 或更高） |
| 去哪装 | https://www.blender.org/download/ |
| 装好验证 | 跟着 Blender 官方入门教程建出一个立方体，并试着给它改名 |

> 建议第一天就把 Blender 界面里的**移动 / 旋转 / 缩放**和**对象命名**在哪儿操作搞熟——第 4 周要靠它出 3D 泵房，命名对不上前端就加载不出来。

### ⑤ 前端

| | |
|---|---|
| 装什么 | **Node.js**（装 LTS 版） |
| 去哪装 | https://nodejs.org/ |
| 装好验证 | 命令行输入 `node -v`，出现版本号（如 `v20.x` 或 `v24.x`） |
| three.js | **不用现在装**。第 3 周建项目时用 `npm install three` 装进项目里 |

> 装 Node 会顺便装好 `npm`，不用单独装。

### ⑥ 产品经理

**不用装任何开发工具。** 有 Office / WPS 和浏览器就够了。

---

## 第 3 步：装完发这组截图给组长

在命令行里依次跑这三行，把结果截图发群里：

```
python --version
pip --version
node -v
```

（做三维的组员如果没有 Node 也没关系，前两行有就行。）

---

## 卡住了怎么办

1. **卡超过 1 小时，就发群里问**，不要一个人闷三天。这是本项目的头号风险，不是能力问题。
2. **截图要完整**——别只截最后一行报错，往上多截几行，关键信息常在中间。
3. 问的时候说清三件事：**我装的是什么 → 我做到哪一步 → 报的什么错**。

---

## 提前说清楚：这些东西**不用装**（别浪费时间找）

| 不用装 | 原因 |
|---|---|
| MySQL / InfluxDB | 本项目用 Python 自带的 SQLite，零安装 |
| "Modbus 协议" | 协议不是软件，不用装 |
| three.js（全局） | 第 3 周建项目时随项目装 |
| Docker | 本项目不需要容器 |

---

## 版本记录

| 版本 | 日期 | 修改人 | 修改内容 |
|---|---|---|---|
| v1.0 | 2026-09-24 | 组长（兼 PM） | 初始版本。按六个角色列出所需软件、验证方法与常见坑 |
